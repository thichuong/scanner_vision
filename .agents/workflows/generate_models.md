---
description: Generate JSON serializable models using build_runner
---

1. Run the build_runner command to generate code for models:
```bash
dart run build_runner build --delete-conflicting-outputs
```
// turbo

2. Verify that `*.g.dart` files are created or updated correctly.
